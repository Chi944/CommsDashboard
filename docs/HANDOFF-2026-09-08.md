# Market workspace audit — 2026-09-08

Implemented in the commit containing this handoff. Check `git log -5 --oneline`
and `git status --short` to distinguish committed source from later local edits.
The cross-repo release manifest is maintained in the knowledge-base repository
at `scripts/MARKET-WORKSPACE-HANDOFF.md` after publication.

## Connections

- Comms: https://comms-dashboard-navy.vercel.app/
- Stock Research: https://stock-research-ecru.vercel.app/
- Research Lab: https://stock-research-ecru.vercel.app/#research-lab
- Trading: http://127.0.0.1:8642/ on the operator's machine.

Workspace navigation uses the same tab. Overview independently loads the public
Stock Research `run.json`: eight-second deadline, bounded response, omitted
credentials, v2 contract validation, UTC as-of freshness from `run_id`, retry and
last-good display. Neither a research error nor an unavailable MiroFish blocks
markets. Stock charts link to `?ticker=SYMBOL` in the research ledger. This is
research interoperability, not portfolio/account synchronization or trade input.

## Fixes

Malformed saved watchlists, portfolios, alerts and currencies no longer crash
startup. Valid rows survive alongside rejected corrupt rows. Alert notification
side effects moved out of deferred React state updaters; StrictMode/remount tests
verify delivery without duplicate notifications. History range validation rejects
prototype-property names and malformed provider points no longer poison charts.

## Verification

- `npm test`: build, 596 Node tests and 107 UI tests passed.
- `npm audit`: zero vulnerabilities.
- Public production smoke and 11 route checks passed against the pre-push site.
- Existing production browser suite: 33 passed.
- Updated local build verified with real public API/research responses at
  390/768/1440 pixels: summary and NVDA deep link correct, no overflow/runtime errors.

After pushing, confirm Vercel deployed the new SHA and rerun the public smoke.
Do not equate pre-push production checks with the new deployment. No paid
generation or fresh broker operation was part of this work. `cursor-handoff.md`
is preserved historical context; this document describes the current release.
